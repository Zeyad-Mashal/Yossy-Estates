function toggleMenu() {
    const menu = document.querySelector('.menu');
    menu.classList.toggle('open');
}
function contact_done() {
    const contact_done = document.querySelector('.contact_submit');
    contact_done.style.display = "block";
}